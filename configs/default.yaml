# Verify prices against https://platform.claude.com/docs/en/about-claude/pricing
# before publishing any dollar figure. Rates below were checked 2026-09-10.
seed: 13
test_size: 0.3
sufficiency_margin: 0.0

small:
  name: haiku
  model_id: claude-haiku-4-5
  input_usd_per_mtok: 1.0
  output_usd_per_mtok: 5.0
  max_tokens: 1024
  supports_temperature: true
  temperature: 0.0

large:
  name: opus
  model_id: claude-opus-5
  input_usd_per_mtok: 5.0
  output_usd_per_mtok: 25.0
  max_tokens: 1024
  # Opus 5 / Sonnet 5 reject temperature and top_p. Sending them is a 400.
  supports_temperature: false
  temperature: 0.0

paths:
  data: data
  artifacts: artifacts
  reports: reports
