# Cheaper contrast (2x price gap instead of 5x). Useful for showing that the
# savings headline is a function of the price ratio, not of the router.
seed: 13
test_size: 0.3
sufficiency_margin: 0.0
small:
  name: haiku
  model_id: claude-haiku-4-5
  input_usd_per_mtok: 1.0
  output_usd_per_mtok: 5.0
  max_tokens: 1024
  supports_temperature: true
large:
  name: sonnet
  model_id: claude-sonnet-5
  input_usd_per_mtok: 2.0
  output_usd_per_mtok: 10.0
  max_tokens: 1024
  supports_temperature: false
paths: {data: data, artifacts: artifacts, reports: reports}
