# Core — needed for training, simulation, and serving
numpy>=1.26,<3
pandas>=2.1
scikit-learn>=1.4
scipy>=1.11
joblib>=1.3
PyYAML>=6.0
matplotlib>=3.8

# Serving
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.6

# Model provider (only needed for --provider anthropic)
anthropic>=0.40

# Benchmark loading (only needed for --sources gsm8k mmlu)
datasets>=2.18

# Dev
pytest>=8.0
httpx>=0.27  # also used by the OpenAI-compatible provider
